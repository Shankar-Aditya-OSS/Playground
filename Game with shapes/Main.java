#include<iostream>
using namespace std;
int main()
{
  int rad,sqr;
  cin>>rad>>sqr;
  rad*=2;
  if(rad<=sqr)
    cout<<"circle can be inside a square";
    else
      cout<<"circle cannot be inside a square";
      
      return 0;
  //Type your code here.
}